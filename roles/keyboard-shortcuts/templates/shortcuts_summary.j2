# roles/keyboard-shortcuts/templates/shortcuts_summary.j2
{# Double space for better readability #}
╔════════════════════════════════════════════════════════════════════════════════╗
║                      KEYBOARD SHORTCUTS CONFIGURATION                           ║
╚════════════════════════════════════════════════════════════════════════════════╝

CONFIGURED SHORTCUTS:
{% for item in shortcuts_result.results %}
  • {{ item.item.desc }}
    Shortcut: {{ item.item.value }}
    Command:  {{ item.item.command }}

{% endfor %}

MANUAL CONFIGURATION:
To set these shortcuts manually, use:
{% for item in shortcuts_result.results %}
  $ {{ item.item.command }}
{% endfor %}

WARNINGS AND NOTES:
  ⚠️ Language Switching:
     In Linux Mint 22, the default language switching shortcut is Ctrl + Win
     This must be configured manually through system settings

VERIFICATION:
To verify current shortcuts:
{% for item in shortcuts_result.results %}
  $ dconf read {{ item.item.key }}
{% endfor %}